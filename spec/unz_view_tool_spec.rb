require "spec_helper"

describe UnzViewTool do
  it "has a version number" do
    expect(UnzViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
